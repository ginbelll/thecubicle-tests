webdriver {
  driver = chrome
  autodownload = true
  timeouts.implicitly_wait = 20000
}

chrome {
  switches = "--incognito, --remote-allow-origins=*, --start-maximized"
}

headless.mode = false

serenity {
  take.screenshots = FOR_FAILURES
  logging = VERBOSE
  timeout = 20
}
